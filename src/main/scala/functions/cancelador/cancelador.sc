class Cancelador {
  def cancelar(subscripcion: Subscripcion): Unit = {}
}

class Subscripcion


val cancelador = new Cancelador

val subscripcion = new Subscripcion

cancelador.cancelar(subscripcion)